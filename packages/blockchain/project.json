{
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "packages/blockchain/src",
  "projectType": "application",
  "targets": {
    "sync-types": {
      "executor": "nx:run-commands",
      "options": {
        "command": "node packages/blockchain/syncTypes.js"
      }
    },
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "cd packages/blockchain/ && anchor build"
      }
    },
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "cd packages/blockchain/ && anchor build"
      }
    },
    "deploy": {
      "executor": "nx:run-commands",
      "options": {
        "command": "cd packages/blockchain/ && anchor build"
      }
    }
  },
  "tags": []
}
